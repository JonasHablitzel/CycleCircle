{
  "name": "cyclecircle",
  "version": "0.0.0",
  "scripts": {
    "dev": "yarn compile-sass && vite",
    "build": "yarn compile-sass && vite build",
    "preview": "yarn compile-sass && vite preview",
    "compile-sass": "node-sass styles/style.scss styles/style.css"
  },
  "devDependencies": {
    "node-sass": "^7.0.1",
    "vite": "^2.7.2"
  },
  "dependencies": {
    "sass": "^1.49.0"
  }
}
